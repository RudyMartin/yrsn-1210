# ============================================================================
# YSRN Configuration Template
# ============================================================================
#
# INSTRUCTIONS:
# 1. Copy this file: cp .env.template .env
# 2. Fill in your actual credentials below
# 3. The .env file is already in .gitignore and will NOT be committed
# 4. NEVER commit files with real credentials to git!
#
# ============================================================================

# ============================================================================
# Application Configuration
# ============================================================================
YSRN_ENV=development
YSRN_APP_NAME=YSRN
YSRN_VERSION=0.1.0

# ============================================================================
# Encoder Configuration
# ============================================================================
# Options: simple, sentence_transformers, openai
# - simple: Fast, no dependencies, for testing only
# - sentence_transformers: Local embeddings, free, requires model download
# - openai: Cloud embeddings, requires API key, costs money
YSRN_ENCODER_TYPE=sentence_transformers

# Sentence Transformers Configuration (if using sentence_transformers)
# Model options: all-MiniLM-L6-v2 (384-dim, fast), all-mpnet-base-v2 (768-dim, better)
YSRN_ENCODER_MODEL=all-MiniLM-L6-v2
YSRN_ENCODER_DEVICE=cpu

# OpenAI Configuration (if using openai encoder)
# Get your API key from: https://platform.openai.com/api-keys
# ⚠️ ENTER YOUR API KEY BELOW (remove the placeholder text)
OPENAI_API_KEY=sk-ENTER-YOUR-OPENAI-API-KEY-HERE
OPENAI_ORGANIZATION=

# Alternative: Set via YSRN_ENCODER_API_KEY instead
# YSRN_ENCODER_API_KEY=sk-ENTER-YOUR-API-KEY-HERE

# ============================================================================
# Database Configuration
# ============================================================================
# Options: in_memory, file, chromadb
# - in_memory: Fast, no persistence, for testing
# - file: JSONL files, simple, local development
# - chromadb: Vector database, production-ready
YSRN_DB_TYPE=chromadb

# File/ChromaDB storage path (relative to project root)
YSRN_DB_PATH=./data/chroma

# ChromaDB collection name
YSRN_DB_COLLECTION=ysrn_contexts

# For remote ChromaDB server (optional - leave empty for local)
# YSRN_DB_HOST=
# YSRN_DB_PORT=

# ============================================================================
# Event Bus Configuration
# ============================================================================
# Options: in_memory, redis, kafka
# - in_memory: Fast, single-process, for testing
# - redis: Distributed, production-ready
# - kafka: Enterprise, high-throughput
YSRN_EVENT_BUS_TYPE=in_memory

# Redis Configuration (if using redis)
# YSRN_EVENT_BUS_HOST=localhost
# YSRN_EVENT_BUS_PORT=6379
# YSRN_EVENT_BUS_TOPIC=ysrn_events

# Kafka Configuration (if using kafka)
# YSRN_EVENT_BUS_HOST=localhost
# YSRN_EVENT_BUS_PORT=9092
# YSRN_EVENT_BUS_TOPIC=ysrn_events

# ============================================================================
# Observability Configuration
# ============================================================================
YSRN_METRICS_ENABLED=true
YSRN_TRACING_ENABLED=true
YSRN_LOG_LEVEL=INFO
YSRN_PROMETHEUS_PORT=9090

# Jaeger endpoint for distributed tracing (optional)
# Format: host:port (e.g., localhost:6831)
# JAEGER_ENDPOINT=

# ============================================================================
# Feature Flags
# ============================================================================
# JSON format: {"feature_name": true, "another_feature": false}
# Example: {"enable_caching": true, "beta_features": false}
YSRN_FEATURE_FLAGS={}

# ============================================================================
# Additional Secrets (if needed)
# ============================================================================
# Database password (if using remote database with auth)
# YSRN_DB_PASSWORD=

# Redis password (if using Redis with auth)
# YSRN_REDIS_PASSWORD=

# ============================================================================
# SETUP CHECKLIST
# ============================================================================
# [ ] Copied .env.template to .env
# [ ] Filled in OPENAI_API_KEY (if using OpenAI encoder)
# [ ] Set YSRN_ENCODER_TYPE to your preferred encoder
# [ ] Set YSRN_DB_TYPE to your preferred database
# [ ] Verified .env is in .gitignore (it should be)
# [ ] Tested configuration with: python -c "from dotenv import load_dotenv; load_dotenv(); from ysrn.infrastructure.config import get_config; print(get_config())"
#
# ============================================================================
# SECURITY REMINDERS
# ============================================================================
# ✓ .env file is in .gitignore - it will NOT be committed
# ✓ Never commit files with real credentials
# ✓ Use different keys for dev/staging/production
# ✓ Rotate API keys if exposed
# ✓ Review .gitignore regularly for new credential patterns
#
# ============================================================================
